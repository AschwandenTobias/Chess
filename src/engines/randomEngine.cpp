#include "randomEngine.h"

std::pair<int, int> RandomEngine::generateRandomMove(Chessboard& board, bool isWhite) {
    std::vector<std::pair<int, int>> allMoves;
    std::pair<int, int> move;
    
    return move;
}